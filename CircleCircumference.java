public class CircleCircumference {
    public static void main(String[] args) {
        double radius = 5.0;
        double circumference = 2 * Math.PI * radius;
        System.out.println("Circumference for radius " + radius + " = " + circumference);
    }
}